<h1>Logged in as:<%= cookies[:customer] %> </h1>

<%= form_for @customer do |f| %>

<H1>Enter Your Email</H1>

<div class="field">
	<%= f.label :email  %>
	<%= f.text_field :email  %>
</div>

<%= f.submit %>
	
<% end %> 
