<section class="container my-4">
  <div class="row">
    <div class="col-8">
      <%= render "search" %>
      <%= render "feedback" %>
      <%= render "feedback" %>
    </div>

    <div class="col">
      <%= render "new_idea" %>
      <%= render "filter" %>
    </div>
  </div>
</section>
